import React from 'react';
import { Container } from '@mui/material';

const ViewNote = () => {
  return (
    <Container maxWidth="md">
      {/* Placeholder for note viewing */}
    </Container>
  );
};

export default ViewNote;
